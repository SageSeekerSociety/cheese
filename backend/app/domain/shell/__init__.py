"""壳 (shell): one platform, several products, no forked frontend.

`catalog.py` holds the declarations, `service.py` resolves which one a project
runs under, `schemas.py` is the wire shape the frontend receives.
"""
