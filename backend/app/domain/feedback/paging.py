"""一页多大。分页的**单位**和每一页的**大小**都只写在这里。

这两个数同时属于两层，所以它自己一个模块：它们既是 repository 一次取多少行的上限，
也是 `GET /feedback/{id}/comments` 那条查询参数的默认值。留在 `repositories.py` 里
的话，路由为了拿一个常量就得 import 整个 repository 模块 —— 而架构守卫
（`tests/unit/test_domain_import_guard.py`）挡的正是「路由直接摸领域的数据层」。
为一行常量开一条豁免是最坏的那个选择：越界一点没少，守卫还少了一个样本。

`HOT_SUPPORTS` 留在 `repositories.py` 是**有意的**，别跟着搬过来：它只被查询用，
不出现在任何一条接口表面上，路由不需要它。
"""

#: 一页取多少栋楼。**分页的单位是楼（顶层评论），不是条** —— 客户端要拿到一整栋
#: 才画得出「回复 X」和那根缩进线，所以一页里的一栋楼连同它的回复一起给。
THREAD_PAGE = 50

#: 一栋楼一页取多少条回复。回复可以比楼多得多（一栋楼就是一场讨论），所以它自己
#: 有个上限，「展开更多」按游标往后取下一页。真取完的话 `reply_counts` 里的总数
#: 和手上条数相等，客户端据此知道没有下一页了。
REPLIES_PAGE = 50
