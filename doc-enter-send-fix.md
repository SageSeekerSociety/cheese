# 修复回车误触发送

## 目标
修复前端 bug：输入框没有焦点（或回车本意是给输入法上屏）时，按回车也会把消息发出去。

## 结论 / 修复方案
发送框的回车处理（两处：工作区底部输入条 `WorkspaceView.vue`、聊天面板自带输入条 `ChatPanel.vue`）原来只判断了 `key === 'Enter'`，现在补了两道防线：

1. **输入法组合中的回车不发送**：用拼音等输入法打字时，按回车"上屏"也会产生一次 Enter 按键——这类回车（`isComposing` / `keyCode 229`）现在会被忽略，不再误发消息。
2. **只认来自输入框本身的回车**：回车事件必须真正来自当前持有焦点的发送输入框才会触发发送；从别处冒泡/透传过来的回车一律不处理。

@-补全菜单打开时"回车选中第一项"的行为保持不变。

## 进展
- ✅ 两处 composer 已修改
- ✅ `vue-tsc` 类型检查通过，前端测试 8/8 通过
- ⏳ 待 @张衡 按原来的触发方式实际验证一下是否复现

## 待办
- 张衡验证修复效果；若仍能复现，提供具体操作步骤（当时焦点在哪、是否在用输入法）。
