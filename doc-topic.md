# 文件面板嵌套目录树

## 目标

「文件」面板左侧原来把所有文件平铺成一列（直接显示完整路径），改成按文件夹嵌套分层的目录树，可展开/收起。

## 方案（已确认，已实现）

- 后端接口本来就返回带完整相对路径的文件列表，后端不动。
- 前端（`frontend/src/components/DocPanel.vue` 的文件工具）把路径列表构建成目录树：每层文件夹在前、文件在后，各自按名字排序。
- 文件夹可点击展开/收起（箭头 + 文件夹图标随状态变化），默认全部展开；文件只显示文件名，缩进体现层级。
- 打开的文件保持高亮；点击打开、编辑、保存行为不变，悬停文件仍可看完整路径和大小。

## 进展

- [x] mentor 确认方案
- [x] 实现目录树（只改 DocPanel.vue 一个文件）
- [x] 类型检查、构建、前端测试全部通过

## 待办

- @mentor-1 在「文件」面板实际点一点，确认展开/收起和层级显示符合预期
