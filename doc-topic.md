# 文件面板嵌套目录树

## 目标

「文件」面板左侧目前把所有文件平铺成一列（直接显示完整路径），要改成按文件夹嵌套分层的目录树，可展开/收起。

## 方案（待确认）

- 后端接口已经返回带完整相对路径的文件列表，不用动后端。
- 前端（DocPanel.vue 的文件工具）把路径列表构建成目录树：文件夹在前、文件在后，各自按名字排序。
- 文件夹可点击展开/收起，默认全部展开；文件只显示文件名，缩进体现层级。
- 打开的文件保持高亮，行为（点击打开、编辑、保存）不变。

## 进展

- [ ] 待 mentor 确认方案后动手
