# 现场内容改为 raw

## 目标
现场（施工记录）里的工具卡片改为保留**原始内容**，不再是加工过的摘要。

## 现状定位（为什么现在不是 raw）
现场卡片在**入库那一刻就被加工了**，原始信息直接丢掉，事后想看也看不到：

- 后端 `backend/app/domain/agent/chat.py` 的 `_format_tool_event()`：把每次工具调用压成「中文动词 + 参数预览」两行存进事件块——
  - 工具名被映射成中文（`Bash`→「执行命令」、`Read`→「读文件」…）；
  - 只挑**一个**参数字段做预览，且压掉换行、**截断到 120 字**；
  - 其余参数全部丢弃。
- 前端 `DocPanel.vue` 的现场时间线就按「● 动词 / ⎿ 预览」渲染这两行，没有原始数据可展开。
- 补充：**工具的输出/结果目前完全没有采集**（`service.py` 只捕获了调用，没有捕获返回结果），所以现在的现场只有"调了什么"，没有"返回了什么"。
- 实时推流那条路（`{"type":"tool", name, input}`）其实已经是 raw 的，问题只在**落库**这一步。

## 方案（待确认后动手）
1. 事件块改存 raw：完整工具名 + 完整参数（JSON），不截断、不做中文映射——加工只发生在**展示层**。
2. 前端现场时间线：默认仍显示简洁的一行（可以继续用中文动词做标签），但**点开能看完整 raw 参数**（等宽字体、可展开）。
3. 旧数据（已按"动词+预览"存的块）保持原样兼容渲染。

## 待拍板
- 只要调用**参数** raw，还是连工具的**输出结果**也要采集进现场？后者要多动一层（采集 SDK 的工具返回），改动大一截。

## 进展
- [x] 定位加工点
- [ ] 等确认范围（是否含输出）
- [ ] 后端改落库格式
- [ ] 前端 raw 展示
- [ ] 测试
