## 当前状态：范围待确认（阻塞中）

这个子话题是 2026-08-10 09:11 从 @平台部分问题记录 拆出来的，但**拆分时没有附任务简报，标题也留着默认的「新话题」**，父话题里在那个时间点前后也没有对应的说明消息（最后一条人类消息停在 05:13）。所以「要办哪件事」目前无法从简报推断。

已 @马霄宇 发了带选项的确认请求。确认后我会立刻改标题、重写本文档为真正的目标/约束/下一步，然后开工。

## 候选范围（来自父话题待办里尚未开子话题的条目）

1. **话题成员权限未生效（疑似权限漏洞）+ 话题拥有者可能为空** — 父话题里已标注这两条很可能同源，排查时放一起看。优先级最高（涉及访问控制）。
2. **对话框 @ 提及体验差** — 成员列表不全、不能滚动、只匹配昵称不匹配 username。
3. **左侧项目列表显示异常** — 有时出现重复项，刷新后表现不稳定（<@chiruotong> 反馈）。
4. **子话题应可折叠** — 话题树折叠/展开，需与已落地的话题列表排序兼容（<@chiruotong> 提出）。
5. **「变更提醒」通知未生效** — `notify --kind change_alert` 发出的提醒没送达。

（父话题另有「项目归档功能调研」和「github问题」两条，已各自有子话题，不在候选内。）

## 等待期间已做的预排查（候选 1）

不空等，先对优先级最高的权限/owner 条目摸了个底，结论如下——**这部分可直接回流父话题，不依赖本话题最终范围**：

- 话题创建链路 `TopicService.create()`（<&backend/app/domain/topic/services.py> 第 97–128 行）在建完话题后调用 `TopicMemberService.seed(topic.id, owner_handle=created_by)`，**创建者本来就会被写成 owner**；`TopicRole` 枚举里 `owner` 也明确注释了"不可被移除到只剩空 owner"。
- 实测本话题与父话题的成员表：两者都有 `role=owner` 的 <@maxiaoyu>，没有出现 owner 为空。
- 也就是说「话题拥有者为空」**在正常 UI 创建路径上没有复现**。真要排查得先拿到出问题的那个话题 ID —— 怀疑方向是 `created_by` 为 None 的调用方（如系统/分身自动建的话题），而不是创建逻辑本身漏写 owner。

## 下一步

- [ ] 等 <@maxiaoyu> 确认本话题要办哪一件（已发选项）
- [ ] 确认后：改标题 → 重写本文档为该任务的目标/约束/验收标准 → 开工
- [ ] 顺手：把上面 owner 的预排查结论回流父话题，避免另一个分身重复探路
