# T5 按 #713 补账并删结论卡

## 现在是什么情况

五件事的代码全部写完并推到 `topic/80027df3`（平台仓与 GitHub 双侧同步，HEAD `9b7d808bb`）。ruff / ruff format / pyright 全绿，单测 **3589 passed**，迁移正反双向都在真库上跑过。集成测试选段正在跑，跑完补上。

## 五件事的落点

1. **两个环境设置**（`device_launch.build_screen_launch`）：每块屏幕的 env 里加 `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH=1`（活不嵌套——孙子分身绑不到任何卡，也没有人能寻址它）和 `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS=4`（承接 T4 删掉的驻留槽上限）。`test_device_launch.py` 钉住数值。
2. **split 写卡 block**：`TopicService._card_block` 往**房间主线**写一条 `meta={"platform","action":"split","task_id"}` 的 event。状态**故意不进 meta**——block 是某一刻的定格，一条永远显示 running 的记录比没有状态更糟；渲染方读活的行拿状态（前端现有的 `splitMarkers` 派生渲染不受影响，它读的是 `GET /topics/{id}/tasks`）。
3. **简报进卡**：`tasks` 加 `brief`/`conclusion` 两列（迁移 `a1c4e7b09f36`），split 与「消息升级成活」两条路都把简报写在行上，**不再给活播种文档**——那份文档是全平台唯一没有维护者的文档（分身拿的是房间 token，够不着活自己的文档地址），从播种那刻起就冻在原地。
4. **结论卡整套退役**：模型/表/路由/CLI 子命令/服务/轮末默认采信/60 秒扫描/归档推迟，全部删除，`grep` 归零，迁移里 drop 表并把存量结论搬到活自己那一列。绑定分身的 `SubagentStop` 直接把 `last_assistant_message` 写进 `tasks.conclusion`（**取最后一条覆盖**，**野 id 一个字不落**）。`cheese conclude-task` 改成**收卡**：`status=closed` + `closed_at`，`--conclusion` 可选（只在分身最后那句是半截时用来覆盖）。
5. **`cheese await` 及唤醒路径全删**：CLI 的 `await`/`__await-child`、后端 `awaited_tasks.py`、两条 `background-task` 路由、`stall_signal` 的 `background_tasks` 入参、await 日志目录，全部清零。

## 一个定下来的取舍：cheese check 改成前台同步跑

`check` 原本借 `await` 的后台机制。await 删掉之后它**在前台跑到完**，退出码就是快检的退出码，输出打在终端上并记到树上。理由是 #713 那句「原生的后台任务替代它」——CLI 调不到 harness 的后台，所以长的快检由 agent 自己 `run_in_background`，跑完 harness 会通知它。重活车道照旧占用。<@wangchangxin> 2026-09-06 认可。

## 顺带清掉的过期文字

- `docs/topics/后台任务唤醒话题.md`（整篇是 await 的设计书）删除，引用它的那处交叉引用改指代码。
- SKILL.md 的 `await` 行与 `conclusion` 行删除，`lock heavy` 与 `conclude-task` 两行改写。
- `test_domain_import_guard` 里 conclusion 领域的两条存量债随领域一起消失。

## 禁区（按简报，未动）

PlaceResolver 的 tasks 半边、`/topics/{任务id}` 地址空间、任务的 agent 身份字段（都归 T6）；前端源码；T1/T2 翻译层与归流行为本体；#689/#691 的闸门逻辑。

## 留给后面的（不在 T5 范围内，记下来）

- `chat.py` 的 `_TOOL_VERB`/`_TOOL_ARG` 里还留着 `create_subtopic` / `return_conclusion` 两个**没有实现的旧 SDK 工具**的中文标签，前端 `toolLabels.ts` 同样。是 T5 之前就有的死标签，没动。
- `cheese accept-request` 的帮助文本仍写「递卡后平台会自动跑一遍检查，红了卡片会被打回」，而机器闸门 #296 已退役。同样是既有过期文案，不在本轮范围。
