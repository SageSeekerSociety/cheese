# issue #187（记忆系统）现状核对 · 2026-08-16

@caisongyang 问「#187 现在是什么状态」。下面是对着 main 和 dev 配置逐条核实的结果。

## 一句话

**issue 本身还开着（8/9 之后没人在上面留过一个字）。四步计划里第 1 步已上线，第 2、3、4 步一步没动，仍然卡在同一个东西上：一个真的智谱 embedding key。** 另外有两件计划外的记忆修复在 8/10–8/11 合了，是这一周记忆真正的进展。

## 逐条状态

| # | 计划里的事 | 状态 | 依据 |
|---|---|---|---|
| 1 | checklist 落到平台（进度层） | ✅ **已上线** | `topic_progress` 表（迁移 `c4a17b93d2e8`）、`TopicProgress` 模型、`GET /api/topics/{id}/progress`、chat.py 每次 Task 事件当场落库、前端轮次结束后保留清单。话题「进度层与记忆落地」8/12 验收 |
| 2 | 把 OpenViking 通上电 | ❌ **未开始** | `memory_backend` 默认仍是 `"db"`；dev 的 `.env`（147 行）里**没有任何** memory / viking / embedding 变量 → 线上跑的就是 db 后端；三样东西一样都没到位 |
| 2a | `.viking` 挂载 | ❌ **仍未加** | `deploy/compose/docker-compose.base.yml` 的 backend volumes 只有 uploads / workspaces / git-credentials / apphome。**issue 里点名的「部署一次记忆清零」这个坑，原样还在** |
| 2b | 真智谱 key | ❌ **仍缺** | 仓库和 dev env 里都没有 `OPENVIKING_EMBEDDING_API_KEY`。这是 8/9 就写着的卡点，一周没动 |
| 3 | 观察抽取质量 | ⛔ **无法开始** | 依赖第 2 步 |
| 4 | dreaming（挂在 #183 回收前） | ❌ **未开始** | `backend/app` 全域 grep `dream` 零命中 |

## 计划外但真正有进展的两件（都已合入 main）

- **PR #221**（8/10 合）：`GET /memory` 能列出 agent 自己的记忆池。
- **PR #271**（8/11 合，话题「记忆机制形同虚设」）：
  - `recall` 从「整句 ILIKE 子串」换成关键词切分 + 覆盖度打分。用本项目 63 条真实记忆实测，6 个人话查询从**全部 0 命中**变成全部命中。
  - 注入截断不再静默：超限时明说「另有 N 条更早的记忆没放进来」，并加了 20000 字符预算。
  - 文档/CLI 里「语义搜索」的错误措辞全部删掉。

这两件不在 #187 的四步清单里，但它们解决的正是 #187 第二节说的「记忆几乎没在长」的一部分——**在 db 后端上**。

## 还悬着的两个决定（#271 抛出来、至今没人拍）

1. **记忆作用域**：写入永远落 per-agent 池，注入只读「自己那个池 + 遗留共享池」。同项目第二个 agent handle 写的东西，第一个一条都看不到。当时实测 per-agent 池里 4 条全是项目级基础设施事实（4/4 分错）。三个选项（A 默认共享 / B 读时合并 / C 维持现状）递给了 @wangchangxin，**代码里至今是 C**（`chat.py:1469` 的 `recall_pools` 仍只读两个池）。
2. **显著性分层**：把已经自发长出来的 `[坑]/[约定]/[事实]` 变成字段、注入时分块排序。`MemoryEntry` 里**没有** `kind` 列 → 未实现。

## 一个别被误导的地方

PR #297 标题叫「[cheesex] issue 187」，8/12 已合并——但它的内容是结论卡 / 设备恢复，**跟记忆无关**。它里面那半套进度层实现（`topics.progress` JSONB，迁移 `d41c9b7a2e18`）已被撤掉，最终采用的是独立表那套。别拿这个 PR 当 #187 关掉的依据。

## 下一步只有一个岔口

第 2 步的三样里，`.viking` 挂载和切 backend 都是几行配置，**唯一的真堵点是智谱 key**。要么有人去申请一个（@andylizf / @caisongyang），要么承认短期内不上 OpenViking、把力气放到 #271 抛出的那两个决定上（作用域 + 显著性分层，两件都不依赖任何外部 key）。
