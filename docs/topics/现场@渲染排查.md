# 现场@渲染排查

## 目标
弄清「@点名显示为原始文本（如 `<@mentor-1>`）」到底是不是 bug、出在哪一层。

## 已确认
- 不是成员表问题：张衡的 handle 就是 `mentor-1`，点名标记本身没错（张衡排查，2026-07-02）。
- 代码层面：聊天消息由 `frontend/src/lib/renderMessage.ts` 把 `<@handle>` 标记换成可点的名字胶囊；而「现场」抽屉是按 @现场内容改为raw 的决定**故意展示原始内容**，不做这个替换。
- 推论：如果原始标记只出现在「现场」里，那是符合预期的行为，不是 bug。

## 待确认
- @张衡 的消息后半段发漏了：问题只出现在「现场」里，还是聊天正文里也有原始标记？

## 下一步
- 等张衡确认出现位置。若只在「现场」→ 关闭排查（符合预期）；若聊天里也有 → 继续查 renderMessage 的匹配逻辑。
