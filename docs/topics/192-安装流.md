## 目标

把「连接 GitHub 仓库」接入平台：项目设置页按钮 → GitHub App 安装页 → 回调落库 project↔installation，此后铸造 installation token 按项目查表，不再用写死的环境变量。

## 前提确认

已用诊断确认 #193 已在 main：`app/api/routes/github_token.py`（`/sandbox/github-token`）+ `app/domain/agent/github_app.py`（`GitHubAppTokens`：JWT→access token、只读权限收窄、20 分钟缓冲续期、锁串行化）现成可复用，本卡不重做这部分。`github_app_installation_id` 目前是 `config.py` 里的单租户配置，注释里已经写明"#192 会换成按项目查表"。

alembic 当前单一 head：`a8c2d4e6f901`（team_compute_profile）。

## 范围（本卡净新工作）

1. **新表** `project_git_installations`（project_id FK→projects.id / installation_id / repo / account）+ 迁移，down_revision=`a8c2d4e6f901`
2. **回调路由** `GET /github/app/callback`：校验 HMAC 签名的 state（防伪造，仿 `sandbox_auth.mint_scoped_token` 思路，把 project_id 签进去）+ upsert 落库，redirect 回前端项目设置页（成功/失败都是跳转，不返回 JSON，仿 `users.py` 里 oauth callback 的 try/except-always-redirect 写法）
3. **发起安装的入口** `GET /api/projects/{project_id}/github/install-url`：签发 state，拼 `github.com/apps/cheesex-app/installations/new?state=...` 返回给前端（前端 window.location 跳转)
4. **改造 `github_app.py`**：`github_app_tokens()` 单例换成按 `installation_id` 缓存的字典，新增 `github_app_tokens_for_project(project_id, session)`：查表拿 installation_id → 找/建实例；`app_id`/`private_key_path` 仍是全局配置（App 级凭据，装多个仓库共用同一把私钥）
5. **`/sandbox/github-token` 路由改造**：从 scoped token claims 里的 `p`(project_id) 查表铸造，不再用全局单例
6. **前端**：项目设置页加"连接 GitHub 仓库"入口（按状态展示已连接的 repo/account，未连接则给安装按钮）+ "连接 GitHub 账号"入口（App user-to-server 授权，与现有 `oauth_github_client_id` 登录态是两条独立流程，不复用）
7. **App 配置**：setup_url 需要人工去 GitHub App 设置页填 —— 验收卡里会标出来，我做不了。回调路由已实现为 `GET /github/app/callback`（`backend/app/api/routes/github_install.py:90`），按现有 `OAUTH_GITHUB_REDIRECT_URL` 的写法（`https://api.cheese.example.com/users/auth/oauth/callback/github`）类推，生产环境该填 `https://api.cheese.example.com/github/app/callback`（把 `api.cheese.example.com` 换成实际部署域名）。这一步只能去 github.com/settings/apps/cheesex-app 手工填，芝士做不到。
8. **宿主机 git 凭据退役**：明确不做，标"后续"，理由是这是切生产部署凭据，需要人过一遍眼

## 依赖 / 并行关系

跟卡1（webhook 原语）、卡2（PR 事件唤醒）互相独立，可并行推进。

## 环境注意事项（本卡新踩的坑）

- **本容器和诊断子话题容器都没有 docker/postgres/redis/root**——`tests/conftest.py` 里连库的 fixture 是 autouse 的，unit/integration 目录的区分不影响这条：只要跑 pytest 就要连真库，这两个环境都连不上，两次独立确认，纯沙箱限制，不是代码问题。真正的把关在 `.github/workflows/test.yml`（GitHub Actions 起 postgres service 容器跑 ruff/pyright/pytest 全套）——PR 合并前会跑这个，本卡在沙箱里没法本地复现 pytest 全绿不代表没人把关。
- **诊断子话题一度以为"main 上没有 #192 代码"是坏消息**——其实是预期状态：本卡的提交在本话题自己的分支 `topic/6196e41a`（commit `8f5198ae`）上，还没到"采纳"（cheese 把子话题分支合进 main 的动作，日志里的 `采纳 topic/xxx → main`）那一步，main 当然还没有。用 `git -c safe.directory=<repo> -C <repo> ...` 一次性参数（不改全局 git config，符合"不碰 git config"的约束）绕开 `.jj` 权限问题验证过：`topic/6196e41a` 完整包含本卡 39 个文件的 diff，没有丢工作。以后再怀疑"提交是不是丢了"，先按 `topic/<CHEESE_TOPIC 前8位>` 找本地分支，别急着假设代码没了。

## 进度

- [x] 环境确认：#193 现成代码位置核实完毕，alembic head 核实为 `a8c2d4e6f901`
- [x] 迁移 + 模型 + repository（`e3a94c6f5b18_project_git_installations.py`；`ProjectGitInstallation` 模型；`ProjectGitInstallationRepository.upsert()` 处理 installation_id 冲突）
- [x] 回调路由 + 安装入口路由 + state 签名/验签（`github_install.py`：`/api/projects/{project_id}/github/install-url`、`/github/app/callback`；签名逻辑在 `github_install_state.py`）
- [x] `github_app.py` 改造为按项目查表（`GitHubAppTokens` 按 `installation_id` 缓存实例；`github_app_tokens_for_project()`；`/sandbox/github-token` 已改造）
- [x] 前端两个入口（`ProjectSettingsView.vue` 约535-601行：连接仓库 + 连接账号，两条独立流程，账号连接不复用 `oauth_github_client_id`）
- [x] 单测（伪造 state、installation_id 冲突等场景）+ 集成测试：`test_github_install_state.py`（过期/篡改/跨类型 state 混用）、`test_github_install.py`（含 `test_callback_installation_conflict_with_another_project`）、`test_github_account_link.py`
- [x] ruff format + ruff check：全绿（修了 github_install.py / test_github_account_link.py 两处格式问题）
- [x] pyright：0 errors
- [x] pytest：本地沙箱（本话题容器 + 诊断子话题容器）均无 docker/postgres/redis/root，两次独立确认跑不了；真正验证靠 PR 上的 `test.yml` CI（起真 postgres）。前端 lint/typecheck 也顺带确认过：2 个 lint 错误 + 33 个 typecheck 错误都是 baseline 既有问题（AuditTask.vue no-undef、tiptap 2.x/3.x 类型冲突），不在本卡改动的文件里；frontend build 在两个容器都因 2GiB 内存上限 OOM，环境限制，非本卡引入
- [ ] 递验收卡给 wangchangxin：标注 setup_url 手动步骤、宿主机凭据退役未做、pytest 未能沙箱内本地验证（走 PR CI）三点
