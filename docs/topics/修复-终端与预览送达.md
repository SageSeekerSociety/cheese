> 状态：改完了，检查跑过（细节见下）；PR #255 的合并冲突已解，等重新验收。目标是「容器里发生的事，能真的送到用户浏览器里」。

## 目标

修 P0-2（实时终端 100% 打不开）、P0-3（运行环境预览 100% 失效）、P1-10（故障被伪装成空态），外加三条低成本尾巴。验收标准不是单测绿，是**面板真的能打开**。

## 摸底结论（已读代码确认）

链路上的断点比简报里列的还多一处：

1. **终端缺凭据** — `terminal.py` 的鉴权要求 `?token=`（iframe 设不了请求头），但 `DocPanel.vue` 把后端返回的裸 url 直接塞进 iframe。
2. **生产 nginx 会把这条路吃掉（新发现，简报里没有）** — `frontend/nginx.conf` 的 `location /api/ { proxy_pass http://backend:8081/; }` 固定剥掉一层 `/api`，而 `vite.config.ts` 专门给 terminal 开了「不剥」的例外，nginx 没跟上。即使补上 token，生产环境仍然 404。ttyd 的 `/ws`、`/token` 是按 `location.pathname` 相对推的，所以这条路径必须原样透传。
3. **available 是假的** — `terminal_status` 只看 `docker port` 解析得出端口就报 `available:true`，既不探活也不看调用者有没有凭据；前端于是不退回时间线，只剩白框。
4. **预览查错容器** — `app_preview_url` 只查 `cheesex-sbx-`，而 3000 端口由 `cheesex-tmux-` 发布。
5. **预览地址进不了浏览器** — 就算查对，返回的是 `http://127.0.0.1:<宿主端口>`，绑的是**宿主** loopback。`docs/spec.md` 自己写了这是已知边界（「远程算力节点接入时升级为经后端的反向代理」）。
6. **空态吞故障** — `DocPanel.vue` 两处 `.catch(() => null)`，把「后端报错」「文件读不到」都塌成「芝士还没有指定预览」。

## 改动（7 项全部落地）

| # | 改动 | 落点 |
|---|---|---|
| 1 | iframe URL 拼 `?token=`；子资源用页面加载时下发的 path-scoped cookie 兜底 | `frontend/src/api.ts`、`backend/app/api/proxy.py` |
| 2 | nginx 补「terminal / app 路径不剥 `/api`」的正则例外，与 vite 对齐 | `frontend/nginx.conf` |
| 3 | `terminal_status` 真探活 + 校验调用者凭据，任一不过一律 `available:false` | `backend/app/api/routes/terminal.py` |
| 4 | `app_endpoint` 遍历 tmux / sbx 两个容器名；旧的 `app_preview_url` 删除 | `backend/app/domain/workspace/service.py` |
| 5 | 新增 `/api/topics/{id}/app/{path}` HTTP + WS 反代，`/preview` 返回反代路径 | `backend/app/api/routes/app_preview.py`、`topics.py` |
| 6 | 预览面板三态化：未指定 / 指定了但读不到 / 后端报错，各给各的提示 | `frontend/src/components/DocPanel.vue` |
| 7 | 尾巴三条：应用挂了但容器还在给提示；`readFile` 话题竞态守卫；全屏预览 Esc 可关 | `frontend/src/components/DocPanel.vue` |

两个代理的共用管道（凭据解析、header 卫生、WS 双向泵、探活）抽在 `backend/app/api/proxy.py`，终端和预览共用一份，不是复制粘贴。

## 一处安全取舍

预览 iframe **故意不带 `?token=`**。终端里跑的是 ttyd——我们自己的固定包；而预览框里渲染的是芝士自己写的页面，查询串能被那个页面的 JS 直接读到（`location.search`，即使 sandbox 也读得到），等于把访问者的 session token 交给 agent 生成的代码。所以预览多走一次 `app-session` 握手：用 Authorization 头换一枚 HttpOnly、path-scoped 的 cookie，iframe 的同源子请求自己带上。

## 已知边界（写进了 spec §9.1）

应用挂在 `/api/topics/{id}/app/` 子路径下。代理会把 HTML 里的**根绝对**资源 URL（`src="/x"`）改写到这个前缀，覆盖静态服务器和普通 dev server 首页；但运行时用 JS 拼根绝对 URL 的应用仍需按注入的 `$CHEESE_APP_BASE` 启动（`vite --base=$CHEESE_APP_BASE`）。

## PR #255 的合并冲突（已解）

第一次递卡时 GitHub 拒绝合并（HTTP 405: merge conflicts）——主干在这条分支开工后走了 23 个提交。已把 main 合进 `topic/c4a9ad69`。

七个文件两边都动过，只有一处真冲突：**`frontend/src/api.ts`**。主干把 `getGitLog` 改成了话题作用域（多一个 `topicId` 参数），而我在紧挨着它的上方新增了 `primeAppPreview` / `withSessionToken`——两边的改动都保留。`DocPanel.vue` 里的调用点已经是主干的 `getGitLog(pid, tid)` 形式，与本次改动不冲突。

其余六个（`topics.py`、`workspace/service.py`、`SKILL.md`、`DocPanel.vue`、`cx_types.ts`、`vite.config.ts`）文本自动合上，逐个复核过语义：nginx / vite 的「terminal 与 app 路径不剥 `/api`」例外、`app_endpoint` 的双容器名遍历、`get_preview` 的 `container_up` 与探活、预览面板三态，合并后都还在。

## 合并仍然没推进（诊断，2026-08-11 06:13）

合并完、推上去、CI 全绿之后，GitHub 对 PR #255 仍然报 `mergeable: false`。查下来**不是分支内容的问题**：

1. **上游没动过。** `POST /projects/{id}/upstream/sync` → `{"synced": true, "commits": 0, "reason": "已是最新"}`。GitHub 的 main 还停在 `4f580bd`（#253）。所以「主干又往前走、又撞上了」这个猜测是错的。
2. **PR 的 live head 就是合并后的提交。** 用 `cheese gh-token` 直接问 GitHub：`594d70df` 的 6 个 check 全 success（05:32 起跑，05:40:26 全部完成）；平台 `pr-checks` 读到的 6 个 job id 与之逐个相同——两个不同的 sha 不可能共享 check-run 的 job id，所以 PR 的头确实是 `594d70df`。
3. **这个头包含 base。** `jj log -r 'main@upstream & ::topic/c4a9ad69'` 返回 `4f580bd`，即 upstream main 是分支头的祖先。分支既然包含 base，就不可能有真冲突。

结论：`mergeable: false` 是过期值，停在 05:32 推送之前。卡片上那条 405 note 写于 05:20，比推送早 12 分钟——**界面上「一直卡在冲突」是这条陈旧 note 造成的观感**。合并那一步从 05:40 CI 转绿到 06:13 之间没有再成功推进过。

沙箱这边查不下去了：只读 token 只放行 `actions/checks`，PR / refs / compare 全是 403，`git ls-remote` 无凭据。再试一次合并仍然 405 的话，就需要在 PR 页面上人工看一眼（例如 base 分支是否被开成了别的东西）。

**顺带发现（不属于本话题，值得单开）**：第一次调 `upstream/sync` 失败，原因是宿主的 main 工作区里有别的话题遗留的未提交文件挡住 checkout——`.github/workflows/box-diag.yml`、`backfill_topic_owners.py`、`test_topic_owner_seed.py`、`docs/topics/话题拥有者为空.md`、`topic/services.py`、`topic_membership/services.py`。这几个话题都已经合进 main 了，残留物却还卡在共享工作区里。第二次调用就过了。

## 检查结果

以下为**合并 main 之后**重跑的结果。

- ruff：全过（713 文件格式一致）。
- pyright：**0 error / 0 warning**。
- 前端 vitest：**293/293 通过**（含新增的 `proxyToken.spec.ts` 4 例；数字比合并前多，是主干带进来的用例）。
- 后端 pytest 全量：**3790 passed / 23 failed / 31 skipped**。23 个失败全部是沙箱缺主机能力，不是代码缺陷：
  - 22 个是 CLAUDE.md 记着的「无 procps，`kill` 二进制不存在」那一组（`test_machine_service.py` 21 个、`test_tmux_control.py` 1 个）；
  - 1 个是 `test_market_api.py::test_market_lists_ai_and_compute_pools`——主干新带进来的用例，断言默认 AI pool `available` 为真，而这取决于 `settings.anthropic_auth_token`，沙箱里没配（实测为空）。主干代码，本次改动没碰过。
- vue-tsc：报错 33 条，与合并前逐条同分布，全部在 tiptap 相关与知是旧页面（`TipTapViewer.vue` 16、`ManageDomainGroups.vue` 6 等），根因是 node_modules 里同时装了 tiptap v2 和 v3 两个大版本——既有问题，本次改动的前端文件零报错。

跑测过程中的环境问题，都不是代码缺陷：vue-tsc 在 2G 的 cgroup 里用 node 默认 1G 堆跑不完会被 OOM kill（要 `NODE_OPTIONS=--max-old-space-size=1700`），它崩掉时在 `frontend/` 留下三个 1.1G 的 core dump，已删（顺手 3.4G）。

## 修掉的一处自伤

抽 `published_endpoint` 公共函数时 `tmux_provider` 不再 import `subprocess`，但 `tests/unit/test_tmux_provider.py` 还在 `monkeypatch.setattr(tp.subprocess, ...)`，两个用例 `AttributeError`。已把桩打到现在真正跑 `docker port` 的那一层，断言不变。

## 还没做的

**没有在真环境里点开过面板**——沙箱里没有 docker，起不了容器，也就没有 ttyd 和真的 app 端口。上面所有绿都只证明逻辑和契约对，**验收标准（面板真的能打开）要在有 docker 的环境上人工确认**：开一个话题 → 施工现场看实时终端能不能出画面 → 让芝士 `cheese serve` 起个 dev server 看预览窗口能不能出页面。
