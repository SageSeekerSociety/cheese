> 状态：改完了，检查跑过（细节见下），等验收。目标是「容器里发生的事，能真的送到用户浏览器里」。

## 目标

修 P0-2（实时终端 100% 打不开）、P0-3（运行环境预览 100% 失效）、P1-10（故障被伪装成空态），外加三条低成本尾巴。验收标准不是单测绿，是**面板真的能打开**。

## 摸底结论（已读代码确认）

链路上的断点比简报里列的还多一处：

1. **终端缺凭据** — `terminal.py` 的鉴权要求 `?token=`（iframe 设不了请求头），但 `DocPanel.vue` 把后端返回的裸 url 直接塞进 iframe。
2. **生产 nginx 会把这条路吃掉（新发现，简报里没有）** — `frontend/nginx.conf` 的 `location /api/ { proxy_pass http://backend:8081/; }` 固定剥掉一层 `/api`，而 `vite.config.ts` 专门给 terminal 开了「不剥」的例外，nginx 没跟上。即使补上 token，生产环境仍然 404。ttyd 的 `/ws`、`/token` 是按 `location.pathname` 相对推的，所以这条路径必须原样透传。
3. **available 是假的** — `terminal_status` 只看 `docker port` 解析得出端口就报 `available:true`，既不探活也不看调用者有没有凭据；前端于是不退回时间线，只剩白框。
4. **预览查错容器** — `app_preview_url` 只查 `cheesex-sbx-`，而 3000 端口由 `cheesex-tmux-` 发布。
5. **预览地址进不了浏览器** — 就算查对，返回的是 `http://127.0.0.1:<宿主端口>`，绑的是**宿主** loopback。`docs/spec.md` 自己写了这是已知边界（「远程算力节点接入时升级为经后端的反向代理」）。
6. **空态吞故障** — `DocPanel.vue` 两处 `.catch(() => null)`，把「后端报错」「文件读不到」都塌成「芝士还没有指定预览」。

## 改动（7 项全部落地）

| # | 改动 | 落点 |
|---|---|---|
| 1 | iframe URL 拼 `?token=`；子资源用页面加载时下发的 path-scoped cookie 兜底 | `frontend/src/api.ts`、`backend/app/api/proxy.py` |
| 2 | nginx 补「terminal / app 路径不剥 `/api`」的正则例外，与 vite 对齐 | `frontend/nginx.conf` |
| 3 | `terminal_status` 真探活 + 校验调用者凭据，任一不过一律 `available:false` | `backend/app/api/routes/terminal.py` |
| 4 | `app_endpoint` 遍历 tmux / sbx 两个容器名；旧的 `app_preview_url` 删除 | `backend/app/domain/workspace/service.py` |
| 5 | 新增 `/api/topics/{id}/app/{path}` HTTP + WS 反代，`/preview` 返回反代路径 | `backend/app/api/routes/app_preview.py`、`topics.py` |
| 6 | 预览面板三态化：未指定 / 指定了但读不到 / 后端报错，各给各的提示 | `frontend/src/components/DocPanel.vue` |
| 7 | 尾巴三条：应用挂了但容器还在给提示；`readFile` 话题竞态守卫；全屏预览 Esc 可关 | `frontend/src/components/DocPanel.vue` |

两个代理的共用管道（凭据解析、header 卫生、WS 双向泵、探活）抽在 `backend/app/api/proxy.py`，终端和预览共用一份，不是复制粘贴。

## 一处安全取舍

预览 iframe **故意不带 `?token=`**。终端里跑的是 ttyd——我们自己的固定包；而预览框里渲染的是芝士自己写的页面，查询串能被那个页面的 JS 直接读到（`location.search`，即使 sandbox 也读得到），等于把访问者的 session token 交给 agent 生成的代码。所以预览多走一次 `app-session` 握手：用 Authorization 头换一枚 HttpOnly、path-scoped 的 cookie，iframe 的同源子请求自己带上。

## 已知边界（写进了 spec §9.1）

应用挂在 `/api/topics/{id}/app/` 子路径下。代理会把 HTML 里的**根绝对**资源 URL（`src="/x"`）改写到这个前缀，覆盖静态服务器和普通 dev server 首页；但运行时用 JS 拼根绝对 URL 的应用仍需按注入的 `$CHEESE_APP_BASE` 启动（`vite --base=$CHEESE_APP_BASE`）。

## 检查结果

- ruff：全过（693 文件格式一致）。
- pyright：325 文件 **0 error / 0 warning**（`check.sh` 那一轮是它自己的 scratch venv 同步超时跳过的，我单独跑通了）。
- 前端 vitest：**221/221 通过**，含新增的 `proxyToken.spec.ts`（4 例）。
- 后端 pytest（全部 unit + 受影响的三个 integration 文件）：**2558 passed / 22 failed**，22 个全部落在 `test_machine_service.py`、`test_tmux_control.py`——CLAUDE.md 记着的「沙箱无 procps，`kill` 二进制不存在」那一组，与本次改动无关。改动涉及的五个测试文件零失败。
- vue-tsc：报错 33 条，全部在 tiptap 相关与知是旧页面（`TipTapViewer.vue`、`ManageDomainGroups.vue` 等），根因是 node_modules 里同时装了 tiptap v2 和 v3 两个大版本——既有问题，本次改动的四个前端文件零报错。

跑测过程中撞到两个环境问题，都不是代码缺陷：宿主磁盘 100% 满（全量 pytest 那轮的 104 个 error 多是 `No space left on device` / asyncpg `DiskFullError`），以及三个检查并行时容器内存被挤爆把 vue-tsc OOM kill 掉。串行重跑后即为上面的结果。

## 修掉的一处自伤

抽 `published_endpoint` 公共函数时 `tmux_provider` 不再 import `subprocess`，但 `tests/unit/test_tmux_provider.py` 还在 `monkeypatch.setattr(tp.subprocess, ...)`，两个用例 `AttributeError`。已把桩打到现在真正跑 `docker port` 的那一层，断言不变。

## 还没做的

**没有在真环境里点开过面板**——沙箱里没有 docker，起不了容器，也就没有 ttyd 和真的 app 端口。上面所有绿都只证明逻辑和契约对，**验收标准（面板真的能打开）要在有 docker 的环境上人工确认**：开一个话题 → 施工现场看实时终端能不能出画面 → 让芝士 `cheese serve` 起个 dev server 看预览窗口能不能出页面。
