## 状态

代码改动已完成;`task check` 里 ruff/pyright 已验证全绿,pytest 在本沙箱内无法执行(见下方阻塞项),需要靠平台质量闸门或有 DB 的环境补跑。

## 已完成

1. **`backend/app/api/routes/users.py:1767`**(`/auth/refresh-token`)—— `create_access_token(user_id)` 改为 `create_access_token(user_id, handle=user.username)`。该路径已经查出了 `user` 对象,直接取 `.username` 即可。
2. **`backend/app/api/routes/users.py:3055`**(`/auth/passkey/verify`)—— 同样补上 `handle=user.username`。
3. 新增测试 `backend/tests/integration/test_auth_token_handle.py`:
   - `test_refresh_token_response_carries_handle` —— 登录后调用 refresh-token,解码返回的 accessToken,断言 `handle` claim 等于用户名。
   - `test_passkey_verify_response_carries_handle` —— 起一个 passkey 认证 challenge,monkeypatch `PasskeyService.verify_authentication` 跳过真实 WebAuthn 签名校验(现有 `test_passkey.py` 也是通过 mock 来测试这条路径,没有真实 authenticator),断言返回 token 带 handle。
4. 未认证兼容通道(`authorize_topic_access` 里 `not actor.authenticated` 直接放行)—— 已确认这是 fusion-design 阶段就写明的 Phase-0 兼容通道,不是本次改动引入的问题;但效果是"不带 token 反而绕过 membership 检查",是一个真实可利用的越权口子。已发决策请求广播给项目全员(`维持现状` vs `现在收紧`),不在本话题范围内擅自改。

## 阻塞项 —— pytest 无法在本沙箱执行

1. **无 DB/Redis 基础设施**:本沙箱没有 `docker` 命令,也没有本地 Postgres/Redis server。默认配置指向 `localhost:5433`(cheesex pg)和 `localhost:6379`(redis),均不可达。`host.docker.internal:5433` 虽然端口开放,但用默认凭据 `cheesex/cheesex` 认证失败——说明那不是本项目的测试库,是宿主机上无关的另一个服务,没有继续试探。
2. **磁盘写满**:排查过程中发现 `/work`(即 `/dev/vda1`)一度被打满到 0 字节可用(`df -h` 显示 100%),导致 `TaskUpdate` 一度因 `ENOSPC` 失败、pytest 缓存写入失败。`du` 统计 `/work`+`/home`+`/usr` 等可见目录总共只有 ~5GB,但 `df` 报告用了 60GB——差额在可见挂载点之外(很可能是宿主机上其它容器/沙箱共享同一块盘),不是本项目代码或本次改动造成的,也不是我能安全清理的范围(未在系统目录做任何删除)。

`ruff check`、`ruff format --check`、`pyright` 均已跑过并全绿(0 errors)。`pytest tests/unit` 尝试跑过一次,2371 个用例全部因连不上 DB 报 `ERROR`(不是断言失败)——确认是环境问题,不是代码回归。

## 下一步

- 需要在有 Postgres(`cheesex_test` @ 5433)+ Redis(6379)的环境(如平台质量闸门,或本机 `task infra` 起的 docker-compose)里补跑一次 `task check` / `bash .claude/scripts/check.sh`,确认新增的两个集成测试通过。
- 未认证兼容通道收紧与否,等父话题拍板后再跟进(如需要,会再拆一个话题去改)。
