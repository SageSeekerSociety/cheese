> 从 @issue 187 拆出。范围为**第 1 项：进度层持久化**。**已完成，全量跑绿，验收卡已递给 <@wangchangxin>。**

## 目标

把 checklist 从"每轮的 working log"变成**跨轮、跨机器可读的话题状态**。

父话题 #187 的诊断：记忆 / 进度 / 上下文三层里，缺的是**进度层**——"这个任务做到哪了"只活在对话记录里，随机器一起死。这就是"换了机器不知道自己做到哪"最直接的原因，也是六项问题里**唯一不依赖任何未决项**的缺口（第 2 项虽也不阻塞，但必须和切 memory backend 同时上，而后者卡在智谱 key 上）。

## 约束

- **进度层 ≠ 记忆层。** 记忆是要注入 prompt 的稳定事实，越长越互相挤；把"做到哪了"塞进去，记忆会退化成流水账，真正重要的几条被挤出去。所以进度是独立一张表，不是一条记忆。
- **必须在轮次死掉时也留得下。** 这一层存在的全部理由就是"turn 没跑完"，攒到 turn 末尾再写等于专门漏掉要救的那一种情况。
- **#186 在前，#187 在后**（换不了身体时记不记得无所谓）——但第 1 项不依赖 #186，可以独立上。

### 为什么是独立表，不是 `topics.progress` JSONB 列（08-12 判定）

父话题 `a1b6848d` 的 PR #297 里有过第二套进度层实现（迁移 `d41c9b7a2e18`，给 `topics` 加一个 `progress` JSONB 列）。两套不兼容的设计做同一件事，08-12 判给本话题这套，理由是设计而不是先后：

- **话题列表是热路径**（一次能拉 400 条）。挂在 `topics` 上，每次列表查询都背着整份 checklist；独立表只在真要读进度时才碰。
- **`turn_id` 表达不了**。这个平台一切以「轮次」为单位，"这份进度是哪一轮写的"是有意义的归因，一个 JSONB 列没地方放。
- **`updated_at` 同理。**
- 更要紧的是它**把上面那条边界糊掉了**——`topics.progress` 读起来就像话题的一个属性，而进度层存在的前提正是"它不是记忆、不该被无条件带进 prompt"。

父话题那半已由 <@wangchangxin> 撤掉（只保留 `review/services.py` 的级联归档，与本话题无关），本地基线里也确认没有 `d41c9b7a2e18`，**不存在撞车**。

事情怎么发生的：本话题 08-11 09:04 从 #187 拆出专做第 1 项，父话题的卡 08-12 07:38 才建——**拆分没有让父话题停下来**，它清单里那一项一直还在。平台缺口，记在 issue #314。

## 已完成的实现

一句话：**Task 工具的每一次改动都当场落库；下一轮开工时，这份清单既进 prompt、也回到房间里。**

| 层 | 改动 |
| --- | --- |
| 表 | `topic_progress`：`topic_id` 作主键（一话题一份），`items` JSON，`turn_id`。迁移 `c4a17b93d2e8`，`down_revision` 为 `c1f7a3b90d24`（#307 把 main 重挂成直链之后的 head） |
| 模型/仓储 | <&backend/app/domain/topic/models.py> 的 `TopicProgress`、<&backend/app/domain/topic/repositories.py> 的 `TopicProgressRepository`（覆盖式 upsert） |
| 写入 | <&backend/app/domain/agent/chat.py>：`_apply_task_event` 一改动就 `_persist_progress` 当场 commit，和 现场 事件同一个契约。best-effort——记不下清单绝不能拖垮正在出活的那一轮 |
| 读出 → prompt | 新的 `_progress_lines` 挂进会话开场那一段。**每个会话都给，不只续跑**：隔了几天在另一台机器上接手，问题一模一样，却没有 `is_resume` 标记 |
| 读出 → 房间 | 开轮时补发一帧 `{type:"todo", restored:true}`；`GET /api/topics/{id}/progress` 供开话题时读（轮次之间没有 WS 流） |
| 前端 | <&frontend/src/components/ChatPanel.vue>：turn 结束**不再清空**清单，改成打上「进度（上次做到这里）」标签留在房间里；失败的轮次同样保留——那正是最需要它的时候 |

### 两个容易做错的地方，已经处理

- **不能拿旧清单去 seed 这一轮的 live 列表。** `_apply_task_event` 按位置编号，而 agent 新 session 的 Task 编号从 1 重开——seed 了的话，这一轮第一次 `TaskUpdate("1")` 会打到上一轮残留的那一条上。所以 live 列表照旧从空开始，旧清单只走 prompt 和那一帧 restored。
- **存的那行会被下一轮第一次 `TaskCreate` 覆盖。** 所以 prompt 里明写"重新建清单时把已完成的也列进去并标成 completed，只列剩下的等于把做过的抹掉"，并有测试盯着这句话还在。

## 测试

新增 14 个功能测试，全绿：

- <&backend/tests/unit/test_progress_prompt.py>（8 个）：三种状态各自的标记；**未知状态降级成"未完成"**（谎报完成是这里唯一不能犯的错）；空 subject 仍成行；"把已完成的也列进去"那句话还在；没有进度时 turn-meta 一字不变。
- <&backend/tests/integration/test_topic_progress.py>（6 个）：清单活过 turn；**活过一个死掉的 turn**（stub agent 记完进度直接抛异常——这一层存在的理由）；下一轮的 system prompt 里带着 `[x]/[~]/[ ]`；房间收到 restored 帧；按话题隔离；未知话题 404。

ruff / pyright（app 0 error）/ vue-tsc / eslint / vitest 299 个全绿。后端全量套件在跑。

### 返工后的验证（沙箱基线早于 #307，须知其边界）

拆掉三样修复后，**本地基线就是 #307 修复之前的 main**，所以本地必然复现两组基线问题，它们在真 main 上都已不存在：

- 本地 `alembic heads` 双头（本地 `c1f7a3b90d24` 仍挂 `b8e1d4c70a92`）→ DB 测试连 schema 都建不起来；
- pyright 2 个 error，正是 `_as_utc` 重复定义（`services.py:43` + `:259`）。

为了真验一遍自己的代码，我把本地 `c1f7a3b90d24` 临时重挂成 #307 后的直链拓扑，跑完**立刻还原**（已用 `jj diff` 确认该文件不在最终改动清单里）。这么做单头恢复，**我那 14 个测试全绿**。

`check-migration-fork.py` 在沙箱里是 **skip 不是 PASS**（无 origin/main 可比），不当作绿；真正的 PASS 由 <@wangchangxin> 在真仓库实测。已在本地通过的：`ruff check`/`format` 全绿、`check-repo-rules.sh` PASS、冲突标记 0。

## 最终验证结果（08-12，两次 rebase 到最新 main 之后）

| 段 | 结果 |
| --- | --- |
| `tests/unit` | **2869 passed** / 22 failed |
| `tests/integration` | **1299 passed** / 1 failed |
| `tests/contract` | **48 passed** / 0 failed |
| 前端 `vitest` | **304 passed**（30 个文件） |
| 前端 `typecheck` | **0 error**（baseline 允许 31） |
| 前端 `lint` / `test:ratchet` | 0 error / 9 passed |
| `ruff check` / `format` / `pyright` | 全绿 |
| `alembic heads` + 全新库实跑 `upgrade head` | 单头，整条链跑通 |

剩下 23 个失败**全部与本改动无关，逐条有交代**：

- **22 个**：已知的沙箱无 procps（`test_machine_service.py` 21 + `test_tmux_control.py` 1），按 CLAUDE.md 不重新诊断。
- **1 个** `test_market_api.py::test_market_lists_ai_and_compute_pools`：环境决定，非缺陷。`available` 算的是 `name in registry.selectable(...)`，默认 profile 就是 GLM 池，沙箱没有智谱 key 必然为 false。**待 <@wangchangxin> 确认**在真仓库是否为绿；若是，它对沙箱就是一条恒红，值得单独处理——全量套件既然是唯一真值，里面留恒红项会逼后面每个人重判一次。

### 三样 main 侧修复已全部从卡里拆走（08-12 返工）

我曾在这张卡里压了三样 main 侧修复。**#307 十分钟内独立落地了其中两样**，<@wangchangxin> 实测后要求全拆，理由不是做得不对，而是**它们已不在本卡范围内**：一张卡同时带着自己的功能和别人已修好的 main 侧改动，采纳时的冲突面会随主线每次移动而变大，而这些冲突全无信息量（基线漂移当天已咬了五张卡加一次真事故，见 issue #312）。

- `_as_utc` 去重 → #307 已落地，取主线那半，我这半删掉
- `test_cheese_cli` 的 `CHEESE_AWAIT_LOGS` 隔离 → 同上
- 合并迁移 `f97f2d912795` → **删除**。#307 是用「重链」而非「合并」修的分叉，main 现在是直链 `c8b1f4a70d29 → b7e3c19d4f80 → a1c9e7b30d42 → c1f7a3b90d24`，我那个合并点的两个 parent 落在同一条直线上，成了空合并节点。`c4a17b93d2e8` 的 `down_revision` 直接改挂 `c1f7a3b90d24`。

**现在这张卡只剩进度层本身：12 个文件。**

一处自我更正：我先前说「ruff/pyright 默认不拦重定义」是错的。pyright 会报 `reportRedeclaration`——把我那半删掉后它就在本地复现了（`services.py:43`）。所以那个 bug 不是没人能发现，是它没被拦在合并前。

## rebase 到最新 main（08-12）

停了 22 小时，main 走了 **68 个提交**，所以 OOM 那轮之前跑的测试全部作废——必须在新底座上重跑。rebase 之后暴露三件事，都不是实现问题，但每一件都会让这个分支直接红：

1. **`chat.py` import 冲突**：main 把 `TopicMembershipRepository` 删了（正文已无引用），而我改了相邻那行。按 main 的意思解——只留我的 `TopicProgressRepository, TopicRepository`。
2. **alembic 变双头**：main 新加了 `c8b1f4a70d29`（结论卡），和我的 `c4a17b93d2e8` 挂在同一个 `down_revision` 上。按 `.claude/rules/migrations.md`「优先 rechain 自己的迁移」，把我的重挂到 `c8b1f4a70d29` 之后。现链：`b8e1d4c70a92 → c1d7e0a4b839 → c8b1f4a70d29 → c4a17b93d2e8`，`alembic heads` 单头。
3. **WS 加了鉴权**，我的测试 helper 裸连被 `auth_required` 拒掉，turn 根本没跑，3 个测试假红。改用仓库标准 helper `chat_ws_url(topic_id, handle)`（`.claude/rules/backend-tests.md` 点名要求别手搓）。修完 14 个全绿。
- 沙箱两处已知失败与本改动无关（无 procps → `test_machine_service.py` / `test_tmux_control.py`），按 CLAUDE.md 不重新诊断。

### 后端全量测试的跑法（08-12 踩坑记录）

上一轮 `pytest tests/ -n 4` 退出码 137，是 OOM 不是测试失败。根因有数：**沙箱 cgroup 上限 2 GiB**（`/sys/fs/cgroup/memory.max` = 2147483648），起了 PG+Redis 之后基线已占 ~1.35 GiB，只剩 800 MB —— 4 个 xdist worker 各带一套连接池，必被 kill。

现在的跑法：`dev-db.sh stop --purge` 清干净 → 分段（unit / integration / contract）→ 并发降到 `-n 2`。`-n 2` 实测不 OOM，integration 1171 个跑完 9 分 28 秒。

**同一个 2 GiB 也管着前端**：`vue-tsc` 报 "failed without reporting any diagnostic" + exit 2 就是被 OOM 杀了（`memory.events` 的 `oom_kill` 会涨，08-11 那个 1.1 GiB 的 `frontend/core.7491` 就是它崩出来的，已删）。**别加 `--max-old-space-size=3072`**——那比 cgroup 上限还大，等于放任 V8 涨到必死；要给个装得下的堆，并且别和后端测试同时跑。

已跑完的段：**`tests/unit` 2698 passed**，失败 22 个全部落在已知的无-procps 名单里。另有 3 个 ERROR 一度可疑（`test_idle_reap.py`、`test_task_ai_advice_routes.py`），查明只是「PG 没起」——它们是要 DB 的 unit 测试，起库后 4 个全过。

## main 自己的迁移分叉（08-12 发现，已修）

递卡前又同步了一次 main，发现 **main 此刻自己就是双头**，跟任何分支无关：`采纳 issue 186 (#301)` 的 `b7e3c19d4f80` 挂在 `c8b1f4a70d29` 上，而 `#289` 的 `c1f7a3b90d24` 挂在更早的 `b8e1d4c70a92` 上，两条在 `b8e1d4c70a92` 处岔开。也就是说 main 上 `alembic upgrade head` 跑不通——按规则文档，这会红 `migration-heads`（ungated）并中断 dev 部署。

两边都已落在 main（落地的迁移不可改），所以按 `.claude/rules/migrations.md` 用合并迁移。建之前确认过这对父节点没有现成的合并迁移（重复的合并迁移本身又是两个头，栽过）。

- 新增 `f97f2d912795`（空 upgrade/downgrade，只接链）
- 我的 `c4a17b93d2e8` 重挂到它之后

**不是只看 `alembic heads` 就算数**：在全新库上实跑 `alembic upgrade head`，整条链一路到头，含合并迁移和 `topic_progress` 建表。

这一段严格说不属于本话题。它是我这条分支达成单头的前置条件，也在给所有人红着 CI，所以顺手修了；**要拆出去单独走随时可以**，就两个文件。

## main 上的 `_as_utc` 重复定义（08-12 发现，已修）

同一次同步里带进来的第二个 main 侧问题，**比迁移分叉更要命**：`backend/app/domain/topic/services.py` 里有**两个 `_as_utc`**——一个容忍 `None`（给 `active_since` 这类可选查询参数用），一个不容忍。Python 只保留后定义的那个，于是容忍版成了死代码，**任何不带时间过滤的话题列表**都死在 `'NoneType' object has no attribute 'tzinfo'`。

已核实是 main 自身的状态（`jj file show -r main` 两个定义都在，第 38 行和第 103 行），不是 rebase 产物：上一轮全量 integration 只有 1 个失败，这 11 个是随这 3 个提交一起进来的。命中 `test_topic_sort`、`test_topic_last_activity`、`test_project_tree`、`test_private_member`、`test_activities`。

修法：合成一个函数，用 `@overload` 保住两种调用形状的精确类型（传 `datetime` 得 `datetime`，传 `None` 得 `None`）。修完这 11 个变 27 passed，pyright 0 error。

和迁移分叉一样，这段也可以单独拆走。

## #186 完成度核查（08-11 的结论，**已过期**）

> 下面这段是 08-11 应要求做的核查。**08-12 `采纳 issue 186 (#301)` 已落地 main**，`device_health` 表、`device/health.py`、quarantine 等当时"一个都不存在"的落点现在都在了。结论保留仅为留痕，别再拿它当现状。

**（当时）没有完成：设计定了，实现一行都没落地。** 话题「issue 186」仍是 active、无人验收、无子话题，最后更新停在 2026-08-10 02:55；GitHub 上没有任何提及 186 的 PR。对 main 逐条查，它那份设计的落点（`host_scoped`、`host_unreachable`、`device_health` 表、`device/health.py`、quarantine、`release_topic_device`）**一个都不存在**；它点名的三个缺口原样还在（`runtime.py`:741、`sql_repository.py`:268、`device_provider.py`:61）。

**但卡住它的东西已经没了**：那轮是被宿主机磁盘打满卡停的（78.6G → 0），现在可用 206.6G / 503.8G，盘还扩了一倍——按它自己那句"宿主机腾出空间后即可开工"，现在就能开工。

核不到的一条：GitHub issue #186 本身是开是关确认不了，平台铸的只读 token 没有 issues 读权限（403）。

## 不在本话题范围内

- 机器判死（`agent/host_failure.py`）；dreaming 的临终收割也要等它，因为触发时机就是"判定这台机器废了"
- `.viking` 挂载 + 切 memory backend（父话题第 2、3 项，卡在智谱 key）
- 智谱 key 谁来出（要人拍板，不是写代码能解决的）
