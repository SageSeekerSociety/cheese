> 从 @issue 187 拆出。拆分时**没有附说明**，父话题文档也没指定要落哪一项 —— 所以下面的"目标"是我按父话题结论推断的，**范围待 <@caisongyang> 确认**（我已在对话里发了选项）。

## 目标（待确认）

父话题 #187 已经把问题盘清楚了（九节分析、六项问题清单），但**没有产出任何改动**。这个子话题应该是"从分析转到落地"的那一步。

父话题的排序结论是：**能立刻动手的只有第 1、2 项**，第 3 项卡在真智谱 key 上，4/5/6 都吊在第 3 项后面。

## 约束（从父话题继承，不重新论证）

- **#186 在前，#187 在后**：换不了身体时，记不记得刚才无所谓。
- **不能把"做到哪了"塞进记忆**：记忆要注入 prompt，越长越互相挤，会把真正重要的几条挤出去。进度层和记忆层是两层。
- **记忆必须中心化 + 强一致**：不能本地副本 + 事后合并；并发控制归存储层（服务端 etag CAS），不靠文件系统约定。
- **`.viking` 挂载和切 backend 必须同时上**：只切 backend 不挂载，观察期会被下一次部署清空，观察到的"记忆很少"分不清是抽取不好还是被清零。
- **dreaming 挂钩点有两条**：`reap_idle_containers`（本地容器）和 `machine/services.py::destroy`（MicroCloud）。只挂一条另一条就是无声消失，且 MicroCloud 才是主路径。

## 我这轮已核实的现状

> 全部对 **main `b9dd6de6`**（2026-08-11 08:57）核的，不是对工作区 —— 本话题的工作副本落后 main 34 个提交，直接 grep 工作区会给出错误结论（#186 那轮踩过同一个坑）。

| 项 | 现状 | 落点 |
| --- | --- | --- |
| 1 进度层缺失 | checklist 只是**每轮的 working log**（"process, not state"），随对话记录一起死，没有任何跨轮/跨机器的持久化 | <&backend/app/domain/agent/chat.py>:194、<&frontend/src/cx_types.ts>:114、<&frontend/src/components/ChatPanel.vue>:504 |
| 2 `.viking` 会被部署清零 | `openviking_data_dir` 默认 `./.viking`，**没有第二处引用**；prod backend 的 volumes 只有 `docker.sock` + workspaces。`.env.prod.example` 只写了 `MEMORY_BACKEND` 的注释，既不设数据目录也不挂载 | <&backend/app/core/config.py>:332、<&deploy/docker-compose.prod.yml>:60-62 |

## #186 完成度核查（应要求）

**结论：没有完成。设计定了，实现一行都没落地。**

平台侧：话题「issue 186」仍是 `active`、`accepted_by` 为空、**没有任何子话题**，最后更新停在 2026-08-10 02:55。GitHub 上**没有任何提及 186 的 PR**。

代码侧（对 main 逐条查）——它自己那份设计的落点，**8 个全都不存在**：

| 设计要的 | main 上 |
| --- | --- |
| `PlatformFailure.host_scoped` 字段 | ✗ 无 |
| `host_unreachable` 失败分类 | ✗ 无 |
| `device_health` 表 + 迁移 | ✗ 无 |
| `backend/app/domain/device/health.py` | ✗ 无（该目录只有原来 6 个文件） |
| quarantine / 隔离 | ✗ 全仓库无此概念 |
| `release_topic_device` | ✗ 无 |

它点名的三个缺口也原样还在：

- `runtime.py`:741-742 —— `if not is_resume and platform_failure is None: resume_after = 5.0`，**认出平台故障就明确不排续跑**
- `sql_repository.py`:268-273 —— `bind_topic_device` 仍是 write-once（"affinity is permanent"）
- `device_provider.py`:61-63 —— 仍写死 "NEVER fall back to another device"

**但当初卡住它的东西已经没了**：#186 那轮是被宿主机磁盘打满卡停的（可用 78.6G → 0，`uv sync` 报 ENOSPC）。现在 **可用 206.6G / 503.8G（已用 59%）**，盘还扩容了一倍。按它自己那句"宿主机腾出空间后即可开工"，**现在就能开工**。

核不到的一条，如实说明：GitHub issue #186 本身是开是关我确认不了 —— 平台铸的只读 token 没有 issues 读权限（403）。上面说的是**平台话题 + 代码**的状态。

### 对本话题的影响

- 第 1、2 项**不受影响**，不依赖 #186。
- 第 5 项（dreaming 临终收割）**受影响**：它的触发时机就是"判定这台机器废了"，而那个判定正是 #186 未落地的部分。父话题第九节第 4 问（判定标准）在 #186 的文档里其实**已经答了**（按 device 计、连续 2 次同一 `host_scoped` 失败、隔离而非销毁），但**只是设计，没有代码**——所以 dreaming 现在连挂钩点都还不存在。

## 下一步

1. **等范围确认**（唯一阻塞项）——我已用选项卡片问 <@caisongyang>：先做进度层持久化、先做 `.viking` 挂载+备份、先出 dreaming 设计，还是另有其事。
2. 范围一定，就直接改代码 + 补测试，不再产出第二份分析文档 —— 父话题那份已经够了。

## 不在本话题范围内

- 换机器/机器判死（归 #186）
- 智谱 key 谁来出（父话题第九节第 1 问，要人拍板，不是写代码能解决的）
