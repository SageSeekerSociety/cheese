> 状态：卡已递给 @fulu，第一次采纳失败（PR #532 和 main 冲突），冲突已解完，**等重试采纳**。

## 采纳失败这件事（2026-08-17）

卡片报的是「PR #532 推送话题分支失败：」——**冒号后面是空的**，等于什么都没说。
查下来实际情况和这句话不一样：

- **推送其实成功了。** PR #532 的 `head.sha` = `c32c4288`，正是当时本地分支的 tip，
  17 个文件 / +1898。分支内容一直好好地在 GitHub 上。
- **真正挡住的是冲突。** #532 当时 `mergeable=false / mergeable_state=dirty`。
- **报错为什么是空的：** `review/services.py` 里把「推分支」和「读 PR head sha」
  塞在同一个 `try` 里，共用一句「推送话题分支失败：{exc}」。`{exc}` 为空
  （httpx 那类异常 `str()` 常常是空串）时这句话就退化成纯噪音，
  而且操作名还可能是错的。**这是一个独立的可诊断性缺陷，不在本卡范围内**
  （`review/services.py` 在本话题的禁区里），要不要修等 @fulu 拍板。

### 分支基线：我之前说错了一次

上一轮我说「分支基于最新 main」，是**读错了** `jj log -r 'main@upstream..topic'`——
那条只说明这些提交不在 main 上，**不代表 main 是祖先**。
真实 merge-base 是 `a9e6e385`，在 #528 之前；分支落后 main 6 个提交。

### 解冲突做了什么

把 GitHub 上的 main（`329f06a0`）合进话题分支，冲突只有两个文件，都在本卡范围内：

- `config.py`：两边在同一处各加各的，**两边都保留**——我的
  `sandbox_signing_secret` + 房间配额，和 #528 的 `agent_api_base()`。
- `tmux_provider.py`：**这里差点静默回滚 #528**。我把 `CHEESE_API` 挪进了房间级
  容器环境（`_room_env`），但值还是旧的 `settings.sandbox_api_base`；只取我这边的话，
  所有 tmux 沙箱会重新拿到多一截 `/api` 的地址——正是我这轮亲身踩到的那个 404。
  已改成 `settings.agent_api_base()`。

`main.py` 是自动合并的，单独核对过语义：#528 加的是 `/api` 相关的新启动告警，
我删的是 `SANDBOX_TOKEN` 那段（已在根上修掉），两处不重叠，合并结果正确。

核对证据：合并后 `merge-tree` 复算无冲突；`main...topic` 的 diff 正好是本卡那 17 个
文件，**没有回滚 main 的任何东西**。分支 tip = `d112fea4`。

> 顺带：我上一轮报的「沙箱 `CHEESE_API` 多一截 `/api`」已经有人在修了——
> #535 `fix(device): stop injecting a doubled /api into device sandboxes`。
> 它只覆盖 device 路径；tmux 路径靠 #528 的 `agent_api_base()` 兜底，本卡合并后保持一致。

## 目标

同一个房间（母话题 + 它派出的所有 task）**共用一个沙箱容器**。
这不是省钱优化，而是 @fulu 2026-08-17「容器不回收」那条决定的**必要前提**：
不回收 × 每话题一个 × 2GB，先崩的是内存。共用之后容器数跟着**房间数**走。

## 做完了什么

**① 容器按房间分配。** 容器名从 `cheesex-tmux-<话题>` 变成 `cheesex-tmux-<房间>`。
话题→房间由 provider 查库解析（task 往上走到 root/topic 那一层），
再把答案落成一个 marker 文件，供 workspace 层那些同步、无 DB 的路径（`docker port` 查询）读。
解析失败一律退回「话题就是自己的房间」——也就是改动前的行为，不会指到别人的容器上。

**② 一容器多会话，per-topic 的东西全部走 tmux 会话环境。**
话题 id、回调令牌、`CLAUDE_CONFIG_DIR`、工作目录、预览端口，一律用
`tmux new-session -e` 逐键写进会话环境。**这条不是洁癖**：tmux 新会话是从
**tmux server 的全局环境**（第一个会话启动时冻结的那份）继承的，不用 `-e` 的话，
房间里第二个话题的 claude 会顶着第一个话题的身份跑、把事件报成那个话题——
device 后端在共享机器上实测过这个事故（`device_launch.py` 里有记录）。

**③ 「后端一重启存量容器全聋」修在根上。** 根因是签名密钥
（`sandbox_auth.SANDBOX_TOKEN`）在没钉 `SANDBOX_TOKEN` 时 fallback 到
**每进程随机**。改成从 `jwt_secret` 派生（带域分隔），重启之间稳定，不需要改部署。
原来那条「发现聋了就重建容器」现在只重建**会话**，不动容器——同房间其他话题不受影响。

**④ 配额提到房间预算**：6GB / 4 CPU / pids 2048（可配）。

**⑤ 顺带修掉两个不修就会立刻炸的东西**（不是附赠，是这次改动的必要条件）：
- `CLAUDE_CONFIG_DIR` 一设，claude 就**不再读** `$HOME/.claude.json`——
  而镜像里烤的首启动门禁正在那儿。不补的话会话会卡在 onboarding 对话框、
  永远等不到 `❯`，每一轮都在 45 秒就绪握手上超时死掉。现在每个话题的 config dir
  里都写一份门禁，并且信任的是**它自己的** cwd（镜像里烤的信任路径是 `/work`，早就不存在了）。
- `docker run -w` 会**创建**不存在的工作目录，而它落在 bind mount 里。
  容器 `-w` 原来指向房间话题的工作区目录——房间没跑过轮次时那目录不存在，
  于是会在宿主机上凭空造一个空目录，正好挡住以后 `jj workspace add` 的位置。
  改成指向 `/topics`（树本身），每个会话用 `new-session -c` 定自己的 cwd。

**⑥ reaper 改成按房间算活跃度。** 容器以房间命名，闲置判断必须取
「房间 + 它所有 task」的最大活跃时间。只看房间的话，一个把活拆出去、
自己安静下来的房间（**这是常态不是边缘情况**）会被连着正在干活的 task 一起回收。

**⑦ 逃生开关** `SANDBOX_SHARE_ROOM_CONTAINER=false`：一键退回每话题一个容器。
共用把房间的故障域合并了（一个话题 OOM 掉容器，同房间全死），
运维撞上时需要一条不用改代码的退路。

## 已定

**idle reaper 保持 8 小时不动**（@fulu 2026-08-17 拍板）。
`sandbox_idle_hours` 这个值这次一行没碰；变的只是「闲置怎么算」——
从「只看房间自己的时间线」改成「房间 + 它所有 task 取最大」。

## 验证（都是我在沙箱里真跑的）

- `bash .claude/scripts/check.sh --no-tests`（= 平台闸门那条命令）→ **7/7 passed**：
  ruff / pyright(0 errors) / alembic 单头 / repo guards / migration fork 全绿。
  pytest 按设计 SKIP（闸门主机没有 Postgres），所以我自己单独跑：
- `tests/unit` → **3367 passed, 1 skipped**
- `tests/integration` + `tests/contract` → **1606 passed, 29 skipped, 0 failed**
- 新增测试：同房间两话题同容器、身份不串（token/topic/hook URL/config dir/cwd 各自独立）、
  容器 env 里不含任何 per-topic 值、端口槽不撞、聋会话只重启会话不动容器、
  签名密钥跨重启稳定、房间有活跃 task 时不回收、房间绑定的各种降级路径。

> 中途 PG 自己挂过一次，导致一批假失败；重启数据库后重跑全绿。这条写出来是因为
> 第一遍的 56 failed 如果只看数字会被当成真失败。

## 已知代价（要接受的，不是待办）

- **故障域合并**：一个话题把容器 OOM 掉，同房间其他话题一起死。今天它们是隔离的。
- **部署那一刻所有话题的 tmux 会话丢一次**：CLI 挂载源从「话题 session 目录」
  移到了「项目 sessions 根」，所以存量容器在下一轮全部会被判为 stale 并重建。
  平台会在房间里发变更提醒说明原因，一次性。
- **运行环境预览/现场终端有槽位上限**：容器创建时一次性发布 N 组端口
  （`SANDBOX_ROOM_PORT_SLOTS`，默认 16），超过的话题照常跑，只是没有独立预览端口。
- **同项目话题之间本来就不隔离**（`sandbox_project_mounts` 的注释里早写了），
  这次没有新增任何访问权限。

## 顺带发现的环境问题（不在这次改动里）

dev 环境的沙箱 `CHEESE_API` 现在是错的：注进来的是 `…/api/api`，
而后端实际在 `…/api` 上（curl 实测 `/api/topics/<id>/doc` → 200，`/api/api/…` → 404）。
后果是容器里所有 `cheese` 命令一律 404——包括递卡、写文档、`cheese ask`。
我这一轮是靠临时 `export CHEESE_API=…/api` 绕过去才递出卡的。
这是**平台侧注入的环境变量**，不是本仓库这次改动引入的，需要单独一张卡修。

## 动过的文件（超出简报禁区的都列在这里）

简报允许 `tmux_provider.py` / `workspace/service.py` / `config.py`。实际还动了四个：
- `core/sandbox_auth.py`（1 行）——验收 #4 的根因修法只能在这里
- `main.py`（删一段已经失效的启动告警）——CLAUDE.md 要求同一次改动清掉过期说法
- `agent/sandbox_notices.py`——「拆容器」和「杀会话」影响范围不同，通知必须分开说
- `scheduler/service.py`——reaper 不改就会误杀正在干活的 task 的容器
禁区列的三个文件（`review/services.py`、`api/routes/`、`agent/chat.py`）**一行没碰**。
