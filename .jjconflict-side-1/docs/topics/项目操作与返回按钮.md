## 待办

- 工作台项目「归档」功能：待 maxiaoyu 确认需求范围（软归档还是级联过滤查询、是否连带冻结 sandbox/agent 运行）后再实现

## 已办

- 梳理平台项目删除现状：AI 工作台项目（uuid，<&backend/app/domain/project>）不支持删除；知是团队项目（int，<&backend/app/domain/team_project>）支持 leader 权限下的软删除
- 项目总览、日历页加了返回按钮（参照设置页面的 `goBack()` 模式，无历史时回退到项目工作台），<&frontend/src/views/OverviewView.vue> <&frontend/src/views/CalendarView.vue>，已过 eslint + vue-tsc
