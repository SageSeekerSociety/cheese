[project]
name = "web-claude"
version = "0.1.0"
description = "Isolated single-purpose server that hosts Claude Code in the browser on top of the generic cheese CLI"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "python-multipart>=0.0.9",
]

[tool.uv]
package = false
