## 目标（一句话）

`conclude` 之后，父话题手上多出一张**有状态、可结算**的结论卡；不结算它也会**默认采信**（父话题那一轮结束时自动 accepted，另加 30 分钟绝对超时），子话题随之归档，不再停在 active。**现有三个副作用（往父话题插消息 / 织进父活文档 / 发 change_alert）一字不改**——本阶段是纯加法。

范围之外，明确不做：结构化栏位（`--card ./conclusion.json`，阶段二）、把父活文档的全文追加改成引用（阶段三）。

## 现场勘察结论（核于 2026-08-11 基线 main@b9dd6de6，换基线后须重验）

> 本节原先标着「不必重查」，而它是在一个落后 main 34 个提交的基线上勘察的。
> 已逐条复核并更正，见下。行号会随 main 漂移，**以符号名为准**。

**量基线落差别写错 revset**（这条坑我踩过，值得留档）：

- `main ~ ::@` —— 拿 main 的**顶点**减去我的祖先集。main 的顶点通常不在我祖先里，所以恒等于 1，看着像「只落后 1 个」。
- `::main ~ ::@` —— main 的**祖先集**减我的祖先集，这才是「落后几个提交」。

危险之处在于**前者不报错**，只给一个看起来合理的小数字，然后把人引向「平台报错了」的错误结论。我就是这么一度认定平台误报，实际是我自己算错。

还要分清两个**不同的量**，别拿一个去否定另一个：

| 量 | 比的是 |
|---|---|
| 「同步上游」`base..upstream/main` | 平台 main ↔ GitHub main |
| `::main ~ ::@` | **本话题分支** ↔ 平台 main |

「同步上游」返回 `commits: 0` 只说明 main 跟 GitHub 齐了；话题分支不跟着 main 自动走，该落后照样落后。

**沙箱里其实能看到最新 main —— CLAUDE.md 那条「你无法与上游同步」需要修正。**
`jj git fetch` 确实用不了（git 2.39 < jj 要求的 2.41，报 `porcelain`），但**卡住的只是
jj 调 git 的那一步，不是网络也不是对象**。jj 工作区底下是一个真的 git 仓库，平台在宿主机
替你 fetch，对象**早就落地了**，只是没导进 jj 的视图：

```bash
git config --global --add safe.directory "$(realpath "$(cat .jj/repo)")/../../.."
jj git import          # 纯本地操作，不碰网络，绕开 porcelain
```

导之前 `main@upstream` 停在 #285，导完直接跳到 `0e5b5fa1` (#293)。**别再靠别人转述 main
的位置**——这条能自己查。（真正做不到的仍然是 push 和主动 fetch：`git fetch upstream` 会
`could not read Username for 'https://github.com'`，凭据只在宿主机那边。）

- `conclude` 的三个副作用在 <&backend/app/domain/topic/services.py>`return_conclusion`（:577-634，原记 :524-567 已漂）；唤醒父话题在路由 <&backend/app/api/routes/topics.py>`:828` 里（原记 :792 已漂），走 `submit_kickoff(..., conclusion_digest_prompt(...))`。
- **alembic head 已两次变更，本条迁移也就 rechain 了两次**。链条现为
  `d4a1b6f27c90 → e7f3a90c15d2 → b8e1d4c70a92 → c1d7e0a4b839 → c8b1f4a70d29(head)`。
  - 第一次：原记的 `d4a1b6f27c90` 是旧基线上的 head，main 早已在它上面接了 `e7f3a90c15d2`。
  - 第二次（2026-08-12）：#286 带进 `c1d7e0a4b839_accept_card_gate_started_at`，父节点**也是** `b8e1d4c70a92`。同一个父节点长出两个孩子**就是**分叉——本条已从 `b8e1d4c70a92` 改挂到 `c1d7e0a4b839`。
  - 教训不是「接错了」，是**长命分支必然会被 main 从底下垫高**。递卡到采纳之间隔了多久，就有多大概率再叉一次；`uv run alembic heads` 要在**合并前**再跑，不是写完跑一次就完事。
- **`check-migration-fork.py` 在 agent 沙箱里是静默空转**（重要，别被它的 PASS 骗了）。它靠 `git ls-tree <base>` 取对照，而 jj 工作区没有 `.git`，于是 `collect()` 抛 `CalledProcessError` → 打一句 `note: origin/main not available — skipping` → **return 0**，check.sh 照样显示 `PASS: merging would not fork the alembic chain`。这是「跳过伪装成通过」的第三例。要让它真跑，得把 GIT_DIR 指到 jj 底下那个真 git 仓库：
  ```bash
  GIT_DIR=$(realpath "$(cat .jj/repo)")/../../.git \
    python3 .claude/scripts/check-migration-fork.py --base upstream/main \
      --versions-dir backend/alembic/versions
  # → PASS: one migration head after merging with upstream/main (66 revisions)
  ```
  - 附带更正：原记「grep 数 `down_revision` 会误判成 4 个」，真实原因不是 `e15e207f8bef` 的多行元组，而是老式无类型标注写法 `down_revision = "..."` 未被匹配；正则补上 `(?::\s*str)?` 后 grep 与 `alembic heads` 结果一致。
- 「一轮结束」没有现成的领域钩子，最靠谱的单点是 <&backend/app/domain/agent/runtime.py>`TurnRunner._execute` 的收尾——它是 transport-independent 的（SDK / tmux / device 共用一条），不会漏掉某个后端。
- 30 分钟绝对超时用 <&backend/app/domain/scheduler/service.py> 里现成的 `PrPollRunner` 同款形状加一个扫描 runner，不碰 `review/`。

## 设计要点（已定，实现按这个来）

- **独立建表 `conclusion_cards`**，收方是新字段 `receiver_topic_id`（父话题）。**不复用 `AcceptCard`**。
- **状态机**：`open` → `accepted` / `returned`（补证据，唯一能回到 open 的分支）/ `escalated` / `superseded`。同一子话题任何时刻最多一张 open 卡。
- **默认采信的四条机制**：
  1. 父话题一轮结束时，**该轮开始前就已存在**的 open 卡自动 accepted（用「轮开始时间」卡一刀，避免把本轮跑到一半才产生、根本没被这一轮看见的卡也吞掉）；另加 30 分钟绝对超时兜底。
  2. 打回必须锚定卡面 uncertainties 里某条 blocking 项 —— 阶段一没有结构化栏位，校验代码写好但对「无结构化栏位的卡」放行，代码里注明这是阶段二才生效。
  3. 每张卡最多打回 1 次（硬上限 2）。
  4. 采信零成本（不唤醒任何人）；打回要付一整轮（唤醒子话题）。
- **打回叫「补证据 need-evidence」**，不叫驳回/rejected。
- **归档语义**：采信即归档子话题；归档前先结算下级未结算的结论卡（否则级联会把孙子连人带卡冻住）；写入顺序先父后子（归档后活文档定格，`edit_doc` 会 400）。
- **路由挂在父话题下**：`POST /api/topics/{parent_id}/conclusion-cards/{card_id}/{accept|need-evidence|escalate}`（per-turn token 按 URL 里的 topic 校验作用域）。三条写路由**必须**进 <&backend/app/main.py> 的 `_CHEESE_WRITE_PATHS`；授权类路由绝不进白名单。

## 结算之后再 conclude：活文档里两段结论会不会打架

会，而且阶段一确实没解决——先把话说清楚，别让它变成默认行为。

**分清两种情况。** `superseded` 只管**结算前**：老卡还开着、还没人看过，新卡直接把它顶掉，父话题只会看见一条，不存在矛盾。**结算后**再 conclude 是另一回事：老结论**已经被采信了**，它已经写进父话题的活文档、已经被后续的轮次读过、甚至可能已经被引用去做了别的决定。那不是一张待处理的卡，是一条**已经生效的事实**。所以新卡不该顶掉它——顶掉等于假装那段历史没发生过。

**问题的准确形态**：现在两段结论会在父活文档里**平铺追加**，没有任何标记说后一段推翻了前一段。读的人（人和 AI 都算）按顺序读下来，只能靠内容自己判断，判断错了就拿旧结论去做决定。这个风险随着子话题反复 conclude 而放大。

**阶段一的现状与止损**：只有一条——`conclusion_cards` 表里同一个子话题的多张卡按 `created_at` 天然有序，且老卡的终态（`accepted`）留着不动。所以「哪条更新」在**数据层是可查的**，只是**活文档层没有表达**。阶段一到此为止，不再多做。

**打算怎么收（阶段三，和「把全文追加改成引用」是同一件事）**：父活文档不再追加结论全文，改成**引用一张卡**。引用是有身份的，同一子话题的第二张卡渲染时就能显式带上「本条取代 <老卡>」，老卡那段自动降级为历史。这个能力必须落在引用机制上——只要活文档里存的还是**没有身份的纯文本**，就没有任何地方能挂「谁取代了谁」，靠措辞约定是拦不住的。

**在那之前，别把它当成已解决**：如果哪个子话题真的反复 conclude 且结论互相矛盾，当前的正确动作是人去父活文档里手工标一句，不要指望机制。

- [x] 新领域 `backend/app/domain/conclusion/`（models / repositories / schemas / services）
- [x] alembic 迁移（接 `b8e1d4c70a92`；**推 PR 前重跑 `uv run alembic heads` 确认仍是 1 个**——main 每落一条迁移就要重接）
- [x] 路由 `backend/app/api/routes/conclusions.py` + `_CHEESE_WRITE_PATHS` 三条
- [x] `return_conclusion` 开卡（三个老副作用不动）+ 消化提示词带上卡面与结算方式
- [x] 轮结束自动采信（`TurnRunner._execute` 收尾）+ 30 分钟超时扫描 runner
- [x] `cheese conclusion accept|need-evidence|escalate`（容器内 CLI，父话题才能真的行使打回/升级）
- [x] 测试：开卡 / 轮结束自动 accepted / 二次 conclude 置 superseded / 有未结算下级卡时的归档
- [x] `task check` 跑绿后递卡

## 验证记录（第二轮，2026-08-12 下午，基线 = main `5fcf3f33` / `upstream/main 0e5b5fa1` #293）

递卡后 wangchangxin 审出「先别合，迁移链会分叉」——**判断正确**，已按下述处理：

- **rebase 到当前 main**：先 `jj git import` 把宿主机已 fetch 的 #286/#293 导进视图（`jj git fetch` 仍然不可用），再 `jj rebase -s <我的根提交> -d main`。落后 5 个提交，不是转述里的 1 个。
- **冲突 2 处**，形态确如预告——两边纯加法，都留：
  - `backend/app/main.py`：main 的 `OrphanSweepRunner`/`GateSweepRunner` vs 我的 `ConclusionSweepRunner`（import 块 + lifespan 启动块各一处）。
  - `backend/app/domain/scheduler/service.py`：main 的 `sweep_abandoned_gates()`/`GateSweepRunner` vs 我的 `sweep_conclusion_cards()`/`ConclusionSweepRunner`。
  - `config.py` 与 `agent/runtime.py` 预告会冲突，实际 jj 自动合掉了；已核对两边内容都在。
- **迁移 rechain 到 `c1d7e0a4b839`**，`uv run alembic heads` → 唯一 head `c8b1f4a70d29`；`alembic history` 确认链条线性。
- **闸门 `check.sh --no-tests`：6/6 PASS**（逐行都是 PASS，不是 SKIP）。另**手工重跑了 fork 守卫**，因为 check.sh 里那行在 jj 工作区是空转（见上文），显式指 GIT_DIR 后才拿到真结果：`PASS ... (66 revisions)`。
- **全量后端套件：4082 passed, 23 failed, 31 skipped**（405s）。**23 条失败全部是宿主缺二进制，与本条线无关**，逐条核过：
  | 文件 | 条数 | 根因 |
  |---|---|---|
  | `tests/unit/test_machine_service.py` | 21 | `FileNotFoundError: 'ssh-keygen'`（缺 openssh-client） |
  | `tests/unit/test_tmux_control.py` | 1 | `FileNotFoundError: 'kill'`（缺 procps） |
  | `tests/unit/test_cheese_cli.py::test_await_log_lives_outside_the_worktree` | 1 | 平台注入的 `CHEESE_AWAIT_LOGS` 短路了 monkeypatch（见下） |

  `grep '^FAILED'` 归类确认**没有一条落在 `conclusion` 上**。沙箱无 root，`apt-get install openssh-client` 装不了，只能记录。

  **顺带更正 CLAUDE.md**：那里写「No procps → 22 failures in `test_machine_service.py`, `test_tmux_control.py`」，归因不准。实测是 **21 条 `ssh-keygen`（openssh-client）+ 1 条 `kill`（procps）**——两个不同的缺失包，`test_machine_service.py` 跟 procps 无关。另外这两个文件在 `tests/unit/` 而非 `tests/integration/`，照 CLAUDE.md 的印象去 `--ignore` 会 ignore 不掉。

## 验证记录（第一轮，2026-08-12 上午，基线 = 当时的 main，落后 0）

- 闸门 `check.sh --no-tests`：**6/6 PASS** —— ruff / pyright(0 errors) / `exactly one migration head` / repo rules / actions pinned / `merging would not fork the alembic chain`。
- 全量后端套件：**4047 passed, 31 skipped**，排除了 `test_machine_service.py` 与 `test_tmux_control.py`（第二轮已查明真实根因，见上节表格；当时误信 CLAUDE.md 归到 procps）。
- 修掉本条线自己的一处：`cheese conclusion` 子命令缺 `description=`，被 main 新增的 `test_every_subcommand_says_what_it_is_for` 抓到，已补。
- 修掉一处真 bug：`sweep_expired` / `accept_stale_for_turn` 遍历同一批卡时，采信上级卡会级联结算下级卡，走到下级时 `_require_open` 抛异常 → 整批回滚、卡永远扫不掉、30 分钟兜底失效。改为跳过已非 open 的卡。

**留给别人的一条**（不在本条线范围内，未改）：`tests/unit/test_cheese_cli.py::test_await_log_lives_outside_the_worktree` 在芝士容器内必挂——它只 monkeypatch 了 `HOME`，没清平台注入的 `CHEESE_AWAIT_LOGS`，于是被真实环境变量短路。`env -u CHEESE_AWAIT_LOGS` 即过；CI 无此变量所以一直绿。补一行 `monkeypatch.delenv("CHEESE_AWAIT_LOGS", raising=False)` 即可。

## 约束

- 主战场 `backend/app/domain/topic/` + 新建领域；**不碰 `backend/app/domain/review/`**（那边有四条线在并行跑）。
- 分层 Route → Service → Repository → Model；SQLAlchemy 2.0 风格；时间列 `DateTime(timezone=True)`，写入 `datetime.now(UTC)`。
- 功能测试，不靠读源码断言实现细节。
