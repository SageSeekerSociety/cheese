"""Cheeselets: server-hosted JS drivers shipped into device screens.

``claude_min.js`` is the minimal driver — it types a turn's prompt into the
interactive ``claude`` and nothing else; perception is via Claude Code hooks.
"""
