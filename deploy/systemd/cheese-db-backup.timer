[Unit]
Description=Run the cheese DB backup every 6 hours

[Timer]
OnCalendar=*-*-* 00,06,12,18:00:00
# Spread load off the exact hour; still runs if the box was down at the mark.
RandomizedDelaySec=300
Persistent=true

[Install]
WantedBy=timers.target
