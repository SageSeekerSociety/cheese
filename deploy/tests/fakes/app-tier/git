#!/usr/bin/env bash
set -euo pipefail

case "${1:-}" in
  fetch)
    exit 0
    ;;
  rev-parse)
    # Real git prints a full sha here; the fixtures name the live tag instead.
    printf '%s\n' "${APP_TIER_GIT_SHA:-${APP_TIER_MAIN_SHA:-testsha}}"
    exit 0
    ;;
esac

exec /usr/bin/git "$@"
